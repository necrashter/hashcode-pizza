# Hashcode Practice Problem: Pizza

Score: **935,275**

## Usage

Cython and Numpy modules are required. Python version is 3.
```
python setup.py build_ext --inplace
python npPizza.py <inputfile> <outputfile>
```
